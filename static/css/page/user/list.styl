@import "../../liblus/_css3";
@import "../../liblus/widget";

// start file
.mod-user-list {
	padding: 20px;
	position: relative;
	.condition-field input {
		max-width: 100px;
	}
	.list {
		clear()
		padding-top: 20px;
		img {
			size(200px);
			border-radius: 3px;
			border: 1px solid #eee;
		}
	}	 
	.page-nav {
		margin-top: 20px;
	}
}

.page-nav {
	@extend .class-page-navi
}
